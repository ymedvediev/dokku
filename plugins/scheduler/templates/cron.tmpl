PATH=/usr/local/bin:/usr/bin:/bin
SHELL=/bin/bash

{{ range $entry := .Commands -}}
{{ $entry.Schedule }} dokku --rm run --scheduler-id {{ $entry.ID }} {{ $entry.App }} {{ $entry.Command }}
{{ end -}}
