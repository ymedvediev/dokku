#!/usr/bin/env bash
set -eo pipefail
set -x

main() {
  declare FILE_NAME="$1"
  export DOKKU_TRACE=1
  export TRACE=1
  mkdir -p test-results/bats
  pushd tests/unit >/dev/null
  bats "$FILE_NAME.bats"
  popd >/dev/null
}

main "$@"
